# Embedded basic-auth style URLs.
https?://[^[:space:]'"]+:[^[:space:]'"]+@

# Password assignments inside code or fixtures.
password[[:space:]]*[:=][[:space:]]*["'][^"']+["']

# Common quoted token/api key style assignments.
(token|api[_-]?key|secret)[[:space:]]*[:=][[:space:]]*["'][^"']+["']
